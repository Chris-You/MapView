@{
	var controller = Context.Request.RouteValues["Controller"].ToString();

	string layout = "";
	if (controller.ToLower() == "camp")
	{
		layout = "~/Views/Shared/_Layout.cshtml";
		
	}
	else
	{
		layout = "~/Views/Shared/_LayoutCharger.cshtml";
	}

	Layout = layout;
}
